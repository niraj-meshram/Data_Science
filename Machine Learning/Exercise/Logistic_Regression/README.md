# Logistic Regression

Overview
- Linear classifier for binary/multiclass problems with probabilistic outputs.
- Covers decision boundaries, regularization, and class imbalance considerations.

What It Does
- Trains logistic regression on HR retention/insurance datasets and reports accuracy/ROC‑AUC.

Run
- Open the notebook and execute; tune `C` (inverse regularization) and class weights.

