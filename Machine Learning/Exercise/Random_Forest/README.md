# Random Forest

Overview
- Ensemble of decision trees trained with bagging and feature randomness.
- Strong baseline for many tabular tasks.

What It Does
- Trains RandomForest on sample datasets and compares to a single tree; reports accuracy/feature importance.

Run
- Open the notebook and execute; tune `n_estimators`, `max_depth`, and `max_features`.

