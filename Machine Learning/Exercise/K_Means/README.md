# K‑Means Clustering

Overview
- Unsupervised clustering algorithm that partitions data into K clusters.
- Demonstrates initialization, inertia, and the elbow method.

What It Does
- Clusters toy datasets (income, iris) and visualizes decision boundaries/centroids.

Run
- Open the notebook and execute; experiment with `n_clusters` and `init`.

