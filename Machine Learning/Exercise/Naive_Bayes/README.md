# Naive Bayes

Overview
- Probabilistic classifiers assuming feature independence (Gaussian/Multinomial/Bernoulli).
- Works well for text and simple tabular data.

What It Does
- Applies NB to spam/titanic/wine datasets and compares to baseline models.

Run
- Open the notebook and execute; choose appropriate NB variant per dataset.

